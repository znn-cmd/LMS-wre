# Миграция на Supabase

Это руководство поможет вам перенести базу данных на Supabase PostgreSQL.

## Шаг 1: Получите пароль базы данных

1. Откройте [Supabase Dashboard](https://supabase.com/dashboard)
2. Выберите проект `cndmouefqjfmxgovrzbx`
3. Перейдите в **Settings** → **Database**
4. Найдите секцию **Database password**
5. Если пароля нет, нажмите **Reset database password** и сохраните новый пароль

## Шаг 2: Создайте файл .env

Создайте файл `.env` в корне проекта со следующим содержимым:

```env
# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://cndmouefqjfmxgovrzbx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_AlCrI62hTdb9ZJ5Rr2X_fg_2NF7YpCr
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImNuZG1vdWVmcWpmbXhnb3ZyemJ4Iiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc2NTg4ODgwNCwiZXhwIjoyMDgxNDY0ODA0fQ.KnUYKUEFZy_iraU4BBEIhRb0628tvrzRoF1N_2y9xmM

# Database (замените [YOUR-PASSWORD] на реальный пароль)
DATABASE_URL="postgresql://postgres:[YOUR-PASSWORD]@db.cndmouefqjfmxgovrzbx.supabase.co:5432/postgres"

# Next.js
NEXT_PUBLIC_APP_URL=http://localhost:3000
```

**Важно:** Замените `[YOUR-PASSWORD]` на ваш реальный пароль базы данных.

## Шаг 3: Проверьте переменные окружения

```bash
npm run check-env
```

Этот скрипт покажет, какие переменные установлены, а какие отсутствуют.

## Шаг 4: Проверьте подключение к базе

```bash
npm run db:test
```

Этот скрипт проверит подключение к Supabase и покажет текущее состояние базы данных.

## Шаг 5: Создайте схему базы данных

```bash
# Сгенерируйте Prisma Client
npm run db:generate

# Примените схему к базе данных Supabase
npm run db:push
```

Команда `db:push` создаст все таблицы, индексы и связи в вашей базе данных Supabase.

## Шаг 6: Заполните базу демо-данными

```bash
npm run db:seed
```

Этот скрипт создаст:
- 5 демо-пользователей (admin, teacher, teamlead, 2 students)
- Курс о Таиланде с 33+ уроками
- Тест с 15 вопросами разных типов
- Прогресс обучения для студентов
- Попытки прохождения тестов

## Шаг 7: Проверьте результат

```bash
# Откройте Prisma Studio для просмотра данных
npm run db:studio
```

Или проверьте через тестовый скрипт:

```bash
npm run db:test
```

## Альтернативный способ: Использование Connection Pooler

Если прямое подключение не работает, используйте Connection Pooler:

1. В Supabase Dashboard: **Settings** → **Database** → **Connection pooling**
2. Скопируйте строку подключения из секции **Connection string** → **Session mode**
3. Используйте её в `DATABASE_URL`

Формат будет примерно таким:
```
postgresql://postgres.cndmouefqjfmxgovrzbx:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres
```

## Troubleshooting

### Ошибка "Environment variable not found: DATABASE_URL"

- Убедитесь, что файл называется именно `.env` (не `.env.local`)
- Убедитесь, что файл находится в корне проекта
- Проверьте, что в `DATABASE_URL` нет лишних пробелов

### Ошибка подключения к базе данных

**P1001: Can't reach database server**

- Проверьте правильность пароля
- Убедитесь, что IP адрес не заблокирован в Supabase
- Попробуйте использовать Connection Pooler

**P1000: Authentication failed**

- Проверьте правильность пароля
- Убедитесь, что используете правильного пользователя (обычно `postgres`)

### Ошибка при применении схемы

Если `db:push` выдает ошибки:

1. Проверьте, что база данных пуста или готова к миграции
2. Если нужно сбросить базу:
   ```bash
   npm run db:reset
   ```
   ⚠️ **Внимание:** Это удалит все данные!

3. Затем снова выполните:
   ```bash
   npm run db:push
   npm run db:seed
   ```

## Проверка через Supabase Dashboard

После выполнения миграции вы можете проверить данные:

1. Откройте Supabase Dashboard
2. Перейдите в **Table Editor**
3. Вы должны увидеть все созданные таблицы:
   - `User`
   - `Course`
   - `Module`
   - `Lesson`
   - `Test`
   - И другие...

## Готово! 🎉

Теперь ваша база данных полностью работает на Supabase. Все данные хранятся в облаке и доступны из любого места.

