# ⚡ Быстрый старт: Деплой на Amvera

## Шаг 1: Подготовка (5 минут)

```bash
# 1. Убедитесь, что все закоммичено
git status

# 2. Закоммитьте изменения (включая amvera.yaml)
git add amvera.yaml amvera.yml
git commit -m "Add Amvera configuration with meta section"
git push origin main
```

⚠️ **ВАЖНО:** Убедитесь, что файл `amvera.yaml` присутствует в корне проекта и содержит секцию `meta`!

## Шаг 2: Создание приложения в Amvera (10 минут)

1. Зайдите на [amvera.ru](https://amvera.ru) и войдите
2. Нажмите **"Создать приложение"**
3. Выберите **"Подключить репозиторий"**
4. Авторизуйтесь через GitHub/GitLab/Bitbucket
5. Выберите репозиторий `wre_lms`
6. Выберите ветку: `main`

## Шаг 3: Настройка переменных окружения (5 минут)

В панели Amvera: **Настройки** → **Переменные окружения**

Добавьте:

```env
NEXT_PUBLIC_SUPABASE_URL=https://cndmouefqjfmxgovrzbx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_AlCrI62hTdb9ZJ5Rr2X_fg_2NF7YpCr
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImNuZG1vdWVmcWpmbXhnb3ZyemJ4Iiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc2NTg4ODgwNCwiZXhwIjoyMDgxNDY0ODA0fQ.KnUYKUEFZy_iraU4BBEIhRb0628tvrzRoF1N_2y9xmM
DATABASE_URL=postgresql://postgres:[ВАШ_ПАРОЛЬ]@aws-0-eu-central-1.pooler.supabase.com:5432/postgres?sslmode=require
NEXT_PUBLIC_APP_URL=https://your-app.amvera.app
```

⚠️ **ВАЖНО:** Замените `[ВАШ_ПАРОЛЬ]` на реальный пароль из Supabase Dashboard!

## Шаг 4: Деплой (5 минут)

1. В панели Amvera нажмите **"Deploy"** или **"Развернуть"**
2. Дождитесь завершения сборки (следите за логами)
3. После успешного деплоя скопируйте URL приложения

## Шаг 5: Обновление URL (2 минуты)

1. Обновите переменную `NEXT_PUBLIC_APP_URL` на реальный URL
2. Перезапустите приложение

## Шаг 6: Проверка (3 минуты)

1. Откройте URL приложения
2. Проверьте, что страница загружается
3. Попробуйте войти в систему

## ✅ Готово!

Ваше приложение развернуто на Amvera!

---

## 🔧 Если что-то пошло не так

### Ошибка: `Configuration file amvera.yaml must contain section 'meta'`

**Проблема:** В конфигурационном файле отсутствует обязательная секция `meta`.

**Решение:**
- Убедитесь, что файл `amvera.yaml` содержит секцию `meta` с указанием окружения:
  ```yaml
  meta:
    environment: node
    toolchain:
      name: npm
      version: 20
  ```
- Закоммитьте файл:
  ```bash
  git add amvera.yaml
  git commit -m "Add meta section to amvera.yaml"
  git push origin main
  ```

### Ошибка: `Found unknown meta.environment: nodejs`

**Проблема:** Использовано неправильное значение `nodejs` вместо `node`.

**Решение:**
- Используйте `environment: node` (не `nodejs`):
  ```yaml
  meta:
    environment: node
    toolchain:
      name: npm
      version: 20  # Версия Node.js, не npm
  ```

### Ошибка: `Invalid variable found: install,build in build section`

**Проблема:** В секции `build` использованы неправильные поля `install` и `build`.

**Решение:**
- Используйте правильную структуру для секции `build`:
  ```yaml
  build:
    skip: false
    additionalCommands: "npm run build"
  ```
- Используйте секцию `run` вместо `runtime`:
  ```yaml
  run:
    command: "npm start"
    containerPort: 3000
  ```

### Ошибка: "В коде не найден указанный файл" в секции run

**Проблема:** Заполнено поле `scriptName`, но файл не существует.

**Решение:**
- **НЕ заполняйте** поле `scriptName` — оставьте его пустым
- Используйте только поле `command` со значением `npm start`
- Остальные поля (`scriptArguments`, `nodeArguments`, `persistenceMount`) оставьте пустыми

### Ошибка: `ENOENT: no such file or directory, open '/source/package.json'`

**Проблема:** Amvera клонирует репозиторий в `/git`, но пытается запустить сборку в `/source`, где нет файлов.

**Решения:**

1. **Убедитесь, что `amvera.yaml` закоммичен:**
   ```bash
   git add amvera.yaml
   git commit -m "Add amvera.yaml configuration"
   git push origin main
   ```

2. **Проверьте настройки в панели Amvera:**
   - Перейдите в **Настройки** → **Сборка** (или **Build Settings**)
   - Убедитесь, что **Корневая директория** (Root Directory) установлена в `/` или пустая
   - Проверьте, что **Команда сборки** (Build Command) указана как `npm run build`
   - Проверьте, что **Команда установки** (Install Command) указана как `npm install`

3. **Если проблема сохраняется:**
   - Попробуйте указать в настройках Amvera явную рабочую директорию
   - Или свяжитесь с поддержкой Amvera для уточнения формата конфигурации

### Ошибка сборки?
- Проверьте логи в панели Amvera
- Убедитесь, что все переменные окружения добавлены
- Проверьте, что `DATABASE_URL` содержит правильный пароль
- Убедитесь, что `package.json` присутствует в репозитории

### Ошибка подключения к БД?
- Проверьте `DATABASE_URL` в переменных окружения
- Убедитесь, что Supabase проект активен
- Попробуйте использовать Connection Pooler URL

### Ошибка: `Failed to fetch 'Inter' from Google Fonts` или `ETIMEDOUT`

**Проблема:** Во время сборки контейнер не может подключиться к Google Fonts API из-за таймаута.

**Решение:**
- Файл `app/layout.tsx` уже настроен с `preload: false` и fallback шрифтами
- Если проблема сохраняется, добавьте переменную окружения в Amvera:
  - `SKIP_GOOGLE_FONTS=true` (опционально, если нужно полностью пропустить загрузку)
- Приложение будет использовать системные шрифты как fallback

### Приложение не запускается?
- Проверьте логи
- Убедитесь, что порт 3000 доступен
- Проверьте команду `npm start` в настройках

---

## 📚 Полная документация

Смотрите [AMVERA_DEPLOY_GUIDE.md](./AMVERA_DEPLOY_GUIDE.md) для подробной инструкции.

