# 🚀 Полная инструкция по развертыванию на Amvera

## Содержание
1. [Подготовка проекта](#1-подготовка-проекта)
2. [Регистрация и настройка в Amvera](#2-регистрация-и-настройка-в-amvera)
3. [Настройка переменных окружения](#3-настройка-переменных-окружения)
4. [Настройка базы данных](#4-настройка-базы-данных)
5. [Развертывание приложения](#5-развертывание-приложения)
6. [Настройка домена](#6-настройка-домена)
7. [Проверка работы](#7-проверка-работы)
8. [Автоматический деплой](#8-автоматический-деплой)
9. [Мониторинг и обслуживание](#9-мониторинг-и-обслуживание)
10. [Решение проблем](#10-решение-проблем)

---

## 1. Подготовка проекта

### 1.1 Проверка файлов конфигурации

Убедитесь, что в корне проекта есть следующие файлы:
- ✅ `amvera.yml` - конфигурация для Amvera
- ✅ `package.json` - с правильными скриптами
- ✅ `next.config.js` - конфигурация Next.js
- ✅ `prisma/schema.prisma` - схема базы данных

### 1.2 Проверка скриптов в package.json

Убедитесь, что в `package.json` есть следующие скрипты:

```json
{
  "scripts": {
    "dev": "next dev",
    "build": "prisma generate && next build",
    "start": "next start",
    "postinstall": "prisma generate"
  }
}
```

**Важно:** Скрипт `postinstall` автоматически генерирует Prisma Client после установки зависимостей.

### 1.3 Локальная проверка

Перед деплоем убедитесь, что приложение работает локально:

```bash
# Установите зависимости
npm install

# Проверьте переменные окружения
npm run check-env

# Запустите локально
npm run dev
```

Откройте `http://localhost:3000` и убедитесь, что всё работает.

---

## 2. Регистрация и настройка в Amvera

### 2.1 Регистрация

1. Перейдите на [amvera.ru](https://amvera.ru)
2. Зарегистрируйтесь или войдите в аккаунт
3. Перейдите в панель управления

### 2.2 Создание нового приложения

1. **Создайте новое приложение**
   - Нажмите "Создать приложение" или "New Application"
   - Выберите тип: **Node.js** или **Next.js**

2. **Подключите Git репозиторий**
   - Если проект на GitHub/GitLab/Bitbucket:
     - Выберите "Подключить репозиторий"
     - Авторизуйтесь через OAuth
     - Выберите репозиторий `wre_lms`
     - Выберите ветку: `main` или `master`
   - Если проекта нет в Git:
     - Сначала загрузите код в репозиторий
     - Затем подключите к Amvera

3. **Настройте базовые параметры**
   - **Имя приложения**: `wre-lms` (или другое)
   - **Среда выполнения**: Node.js 18.x или 20.x
   - **Корневая директория**: `/` (корень проекта)
   - **Команда сборки**: `npm run build`
   - **Команда запуска**: `npm start`

### 2.3 Настройка конфигурации

Amvera автоматически обнаружит файл `amvera.yml` в корне проекта. Убедитесь, что файл создан (см. раздел ниже).

---

## 3. Настройка переменных окружения

### 3.1 Необходимые переменные

В панели Amvera перейдите в **Настройки** → **Переменные окружения** и добавьте:

#### Обязательные переменные:

```env
# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://cndmouefqjfmxgovrzbx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_AlCrI62hTdb9ZJ5Rr2X_fg_2NF7YpCr
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImNuZG1vdWVmcWpmbXhnb3ZyemJ4Iiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc2NTg4ODgwNCwiZXhwIjoyMDgxNDY0ODA0fQ.KnUYKUEFZy_iraU4BBEIhRb0628tvrzRoF1N_2y9xmM

# Database (Prisma)
# ⚠️ ВАЖНО: Замените [YOUR-PASSWORD] на ваш реальный пароль базы данных
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@aws-0-eu-central-1.pooler.supabase.com:5432/postgres?sslmode=require

# Next.js
NEXT_PUBLIC_APP_URL=https://your-app.amvera.app
```

#### Как получить пароль базы данных:

1. Откройте [Supabase Dashboard](https://supabase.com/dashboard)
2. Выберите проект `cndmouefqjfmxgovrzbx`
3. Перейдите в **Settings** → **Database**
4. Найдите **Connection string** или **Database password**
5. Если пароля нет, нажмите **Reset database password**
6. Скопируйте пароль и вставьте в `DATABASE_URL`

#### Рекомендации по DATABASE_URL:

- **Для продакшена**: Используйте Connection Pooler URL (порт 5432 или 6543)
  ```
  postgresql://postgres.cndmouefqjfmxgovrzbx:[PASSWORD]@aws-0-eu-central-1.pooler.supabase.com:5432/postgres?sslmode=require
  ```
- **Формат**: `postgresql://postgres.[PROJECT-REF]:[PASSWORD]@[REGION].pooler.supabase.com:5432/postgres?sslmode=require`

### 3.2 Обновление NEXT_PUBLIC_APP_URL

После первого деплоя:
1. Скопируйте URL вашего приложения на Amvera (например: `https://wre-lms.amvera.app`)
2. Обновите переменную `NEXT_PUBLIC_APP_URL` в настройках Amvera
3. Перезапустите приложение

### 3.3 Безопасность

⚠️ **Важно:**
- Никогда не коммитьте переменные окружения в Git
- Используйте только панель Amvera для настройки переменных
- Храните `SUPABASE_SERVICE_ROLE_KEY` в секрете
- Регулярно обновляйте пароли

---

## 4. Настройка базы данных

### 4.1 Проверка подключения к Supabase

Убедитесь, что:
- ✅ Supabase проект активен
- ✅ База данных доступна
- ✅ Пароль базы данных известен
- ✅ Connection Pooler включен (рекомендуется)

### 4.2 Применение миграций Prisma

После первого деплоя или при обновлении схемы:

**Вариант 1: Через Amvera Console (если доступен)**

```bash
# В консоли Amvera выполните:
npm run db:push
```

**Вариант 2: Локально с указанием DATABASE_URL**

```bash
# Установите переменную окружения
export DATABASE_URL="postgresql://postgres:[PASSWORD]@aws-0-eu-central-1.pooler.supabase.com:5432/postgres?sslmode=require"

# Примените миграции
npx prisma db push

# Или используйте миграции
npx prisma migrate deploy
```

**Вариант 3: Через Supabase SQL Editor**

1. Откройте Supabase Dashboard
2. Перейдите в **SQL Editor**
3. Выполните SQL из `prisma/schema.prisma` (если нужно)

### 4.3 Проверка базы данных

После деплоя проверьте подключение:

```bash
# В консоли Amvera или локально
npm run db:test
```

---

## 5. Развертывание приложения

### 5.1 Первый деплой

1. **Сохраните все изменения в Git**
   ```bash
   git add .
   git commit -m "Prepare for Amvera deployment"
   git push origin main
   ```

2. **В панели Amvera**
   - Перейдите в раздел **Deployments** или **Развертывание**
   - Нажмите **Deploy** или **Развернуть**
   - Amvera автоматически:
     - Клонирует репозиторий
     - Установит зависимости (`npm install`)
     - Выполнит сборку (`npm run build`)
     - Запустит приложение (`npm start`)

3. **Мониторинг процесса**
   - Следите за логами сборки в реальном времени
   - Проверьте наличие ошибок
   - Дождитесь завершения деплоя

### 5.2 Проверка логов

После деплоя проверьте логи:

1. В панели Amvera перейдите в **Logs**
2. Проверьте:
   - ✅ Успешная установка зависимостей
   - ✅ Успешная генерация Prisma Client
   - ✅ Успешная сборка Next.js
   - ✅ Приложение запущено на порту (обычно 3000)
   - ❌ Отсутствие ошибок подключения к БД

### 5.3 Типичные проблемы при первом деплое

**Проблема: Prisma Client не найден**
- **Решение**: Убедитесь, что скрипт `postinstall` есть в `package.json`

**Проблема: Ошибка подключения к БД**
- **Решение**: Проверьте `DATABASE_URL` и доступность Supabase

**Проблема: Переменные окружения не загружены**
- **Решение**: Убедитесь, что все переменные добавлены в панели Amvera

---

## 6. Настройка домена

### 6.1 Использование домена Amvera

По умолчанию Amvera предоставляет домен вида:
- `https://your-app.amvera.app`
- `https://your-app-[random].amvera.app`

Этот домен работает сразу после деплоя.

### 6.2 Подключение собственного домена

1. **В панели Amvera**
   - Перейдите в **Settings** → **Domains**
   - Нажмите **Add Domain**
   - Введите ваш домен (например: `lms.yourcompany.com`)

2. **Настройка DNS**
   - Добавьте CNAME запись:
     ```
     Type: CNAME
     Name: lms (или @ для корневого домена)
     Value: your-app.amvera.app
     ```
   - Или A-запись (если Amvera предоставляет IP):
     ```
     Type: A
     Name: @
     Value: [IP адрес от Amvera]
     ```

3. **Ожидание распространения DNS**
   - Обычно занимает 5-60 минут
   - Проверьте через: `nslookup your-domain.com`

4. **Обновление переменных окружения**
   - Обновите `NEXT_PUBLIC_APP_URL` на новый домен
   - Перезапустите приложение

### 6.3 Настройка SSL

Amvera обычно автоматически настраивает SSL через Let's Encrypt для всех доменов.

---

## 7. Проверка работы

### 7.1 Базовые проверки

1. **Откройте приложение**
   - Перейдите по URL вашего приложения
   - Проверьте, что страница загружается

2. **Проверьте API endpoints**
   - `/api/auth/login` - должен отвечать
   - `/api/courses` - должен возвращать данные (если есть)

3. **Проверьте базу данных**
   - Войдите в систему
   - Создайте тестового пользователя
   - Проверьте работу основных функций

### 7.2 Проверка производительности

1. **Откройте DevTools** (F12)
2. Проверьте:
   - Время загрузки страницы
   - Размер бандла
   - Ошибки в консоли
   - Сетевые запросы

### 7.3 Проверка логов

В панели Amvera проверьте логи на наличие:
- ✅ Успешных запросов
- ❌ Ошибок 500
- ❌ Ошибок подключения к БД
- ❌ Ошибок Prisma

---

## 8. Автоматический деплой

### 8.1 Настройка автоматического деплоя

Amvera поддерживает автоматический деплой при пуше в Git:

1. **В панели Amvera**
   - Перейдите в **Settings** → **Git**
   - Убедитесь, что **Auto Deploy** включен
   - Выберите ветку: `main` или `master`

2. **При каждом push в выбранную ветку**
   - Amvera автоматически обнаружит изменения
   - Запустит новый деплой
   - Уведомит вас о результате

### 8.2 Настройка уведомлений

1. Перейдите в **Settings** → **Notifications**
2. Настройте уведомления о:
   - Успешных деплоях
   - Ошибках деплоя
   - Изменениях статуса

### 8.3 Ручной деплой

Если автоматический деплой отключен:
1. Перейдите в **Deployments**
2. Нажмите **Deploy Now**
3. Выберите ветку и коммит
4. Нажмите **Deploy**

---

## 9. Мониторинг и обслуживание

### 9.1 Мониторинг приложения

В панели Amvera доступны:
- **Логи** - просмотр логов в реальном времени
- **Метрики** - CPU, память, сеть
- **История деплоев** - все версии приложения

### 9.2 Обновление приложения

1. **Внесите изменения в код**
2. **Протестируйте локально**
3. **Закоммитьте и запушьте в Git**
   ```bash
   git add .
   git commit -m "Update: описание изменений"
   git push origin main
   ```
4. **Amvera автоматически задеплоит** (если включен Auto Deploy)

### 9.3 Обновление зависимостей

```bash
# Локально обновите зависимости
npm update

# Проверьте, что всё работает
npm run dev

# Закоммитьте package.json и package-lock.json
git add package.json package-lock.json
git commit -m "Update dependencies"
git push origin main
```

### 9.4 Откат к предыдущей версии

1. Перейдите в **Deployments**
2. Найдите нужную версию
3. Нажмите **Redeploy** или **Rollback**

---

## 10. Решение проблем

### 10.1 Приложение не запускается

**Симптомы:**
- Ошибка 502 Bad Gateway
- Приложение не отвечает

**Решения:**
1. Проверьте логи в панели Amvera
2. Убедитесь, что порт указан правильно (обычно 3000)
3. Проверьте переменные окружения
4. Убедитесь, что команда `npm start` работает

### 10.2 Ошибки подключения к базе данных

**Симптомы:**
- `Can't reach database server`
- `Connection timeout`
- Prisma ошибки

**Решения:**
1. Проверьте `DATABASE_URL` в переменных окружения
2. Убедитесь, что Supabase проект активен
3. Проверьте, что Connection Pooler включен
4. Проверьте firewall настройки Supabase

### 10.3 Ошибки сборки

**Симптомы:**
- Build failed
- Prisma generate errors
- TypeScript errors

**Решения:**
1. Проверьте логи сборки
2. Убедитесь, что все зависимости в `package.json`
3. Проверьте версию Node.js (должна быть 18+)
4. Попробуйте собрать локально: `npm run build`

### 10.4 Переменные окружения не работают

**Симптомы:**
- `process.env.VARIABLE` возвращает `undefined`
- Ошибки "Missing environment variable"

**Решения:**
1. Убедитесь, что переменные добавлены в панели Amvera
2. Переменные с префиксом `NEXT_PUBLIC_` должны быть доступны на клиенте
3. Перезапустите приложение после добавления переменных
4. Проверьте, что нет опечаток в именах переменных

### 10.5 Проблемы с Prisma

**Симптомы:**
- `PrismaClient is not generated`
- Schema errors

**Решения:**
1. Убедитесь, что `postinstall` скрипт есть в `package.json`
2. Проверьте, что `prisma/schema.prisma` корректен
3. Попробуйте выполнить `npx prisma generate` вручную
4. Проверьте версию Prisma в `package.json`

### 10.6 Медленная работа приложения

**Решения:**
1. Проверьте метрики в панели Amvera
2. Увеличьте ресурсы приложения (если доступно)
3. Оптимизируйте запросы к базе данных
4. Используйте кэширование
5. Проверьте размер бандла Next.js

---

## 11. Дополнительные настройки

### 11.1 Настройка окружений (Production/Staging)

Amvera может поддерживать несколько окружений:

1. **Production** - основное приложение
2. **Staging** - тестовое окружение

Настройте разные переменные окружения для каждого окружения.

### 11.2 Резервное копирование

1. **База данных**: Настройте автоматические бэкапы в Supabase
2. **Код**: Храните код в Git (GitHub/GitLab)
3. **Переменные окружения**: Сохраните копию в безопасном месте

### 11.3 Масштабирование

Если приложение требует больше ресурсов:
1. Перейдите в **Settings** → **Scaling**
2. Увеличьте количество инстансов
3. Настройте автомасштабирование (если доступно)

---

## 12. Контакты и поддержка

- **Документация Amvera**: [docs.amvera.ru](https://docs.amvera.ru)
- **Поддержка Amvera**: [amvera.ru/support](https://amvera.ru/support)
- **Документация Next.js**: [nextjs.org/docs](https://nextjs.org/docs)
- **Документация Prisma**: [prisma.io/docs](https://prisma.io/docs)
- **Документация Supabase**: [supabase.com/docs](https://supabase.com/docs)

---

## Чеклист перед деплоем

- [ ] Все файлы закоммичены в Git
- [ ] `amvera.yml` создан и настроен
- [ ] `package.json` содержит все необходимые скрипты
- [ ] Переменные окружения добавлены в панели Amvera
- [ ] `DATABASE_URL` содержит правильный пароль
- [ ] Supabase проект активен и доступен
- [ ] Приложение работает локально (`npm run dev`)
- [ ] Сборка проходит успешно (`npm run build`)
- [ ] Prisma Client генерируется (`npm run postinstall`)
- [ ] Репозиторий подключен к Amvera
- [ ] Автоматический деплой настроен (опционально)

---

## Быстрый старт

```bash
# 1. Подготовка
git add .
git commit -m "Prepare for Amvera"
git push origin main

# 2. В панели Amvera:
# - Создайте приложение
# - Подключите репозиторий
# - Добавьте переменные окружения
# - Нажмите Deploy

# 3. После деплоя:
# - Обновите NEXT_PUBLIC_APP_URL
# - Проверьте работу приложения
# - Настройте домен (опционально)
```

---

**Успешного деплоя! 🚀**

